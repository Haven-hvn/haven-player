[{"max_videos_per_channel": 5}, ["Mock(returncode=1, stderr=\"deno not found", "Deno fails\n                Mock(returncode=0, stdout=\"v20.10.0", "stderr=\"", "Node.js succeeds\n            ]\n\n            runtime_type, runtime_path = youtube_plugin._detect_js_runtime()\n\n            assert runtime_type == \"nodejs\"\n            assert runtime_path == \"node", "assert mock_run.call_count == 2\n\n    def test_detect_js_runtime_none_available(self, youtube_plugin):\n        \"", "Test that None is returned when neither Deno nor Node.js is available.\"", "\n        with patch('subprocess.run') as mock_run:\n            # Both Deno and Node.js fail\n            mock_run.side_effect = [\n                Mock(returncode=1, stderr=\"deno not found", "Deno fails\n                Mock(returncode=1, stderr=\"node not found", "Node.js fails\n            ]\n\n            runtime_type, runtime_path = youtube_plugin._detect_js_runtime()\n\n            assert runtime_type is None\n            assert runtime_path is None\n            assert mock_run.call_count == 2\n\n    def test_detect_js_runtime_deno_timeout(self, youtube_plugin):\n        \"", "Test that timeout is handled gracefully during runtime detection.\"", "\n        import subprocess\n\n        with patch('subprocess.run') as mock_run:\n            mock_run.side_effect = subprocess.TimeoutExpired(\"deno", 5, "runtime_type, runtime_path = youtube_plugin._detect_js_runtime()\n\n            assert runtime_type is None\n            assert runtime_path is None\n\n    def test_detect_js_runtime_deno_not_found(self, youtube_plugin):\n        \"", "Test that FileNotFoundError is handled gracefully.\"", "\n        with patch('subprocess.run') as mock_run:\n            mock_run.side_effect = FileNotFoundError(\"deno not found", "runtime_type, runtime_path = youtube_plugin._detect_js_runtime()\n\n            assert runtime_type is None\n            assert runtime_path is None\n\n    def test_is_js_runtime_available_when_runtimes_detected(self, youtube_plugin):\n        \"", "Test that _is_js_runtime_available returns True when runtime is detected.\"", "\n        youtube_plugin.js_runtime_path = \"deno\"\n        youtube_plugin.js_runtime_type = \"deno", "assert youtube_plugin._is_js_runtime_available() is True\n\n    def test_is_js_runtime_available_when_no_runtime(self, youtube_plugin):\n        \"", "Test that _is_js_runtime_available returns False when no runtime is detected.\"", "\n        youtube_plugin.js_runtime_path = None\n        youtube_plugin.js_runtime_type = None\n\n        assert youtube_plugin._is_js_runtime_available() is False\n\n    def test_get_js_runtime_path_when_available(self, youtube_plugin):\n        \"", "Test that _get_js_runtime_path returns the runtime path when available.\"", "\n        youtube_plugin.js_runtime_path = \"deno\"\n\n        assert youtube_plugin._get_js_runtime_path() == \"deno", "def test_get_js_runtime_path_when_not_available(self, youtube_plugin):\n        \"", "Test that _get_js_runtime_path returns None when no runtime is available.\"", "\n        youtube_plugin.js_runtime_path = None\n\n        assert youtube_plugin._get_js_runtime_path() is None\n\n    @patch('app.plugins.builtin.youtube_plugin.platform.system')\n    def test_get_runtime_installation_guide_macos(self, mock_system):\n        \"", "Test that macOS-specific installation guide is provided.\"", "\n        mock_system.return_value = \"Darwin\"\n\n        plugin = YouTubePlugin()\n        guide = plugin._get_runtime_installation_guide()\n\n        assert \"macOS Installation:\" in guide\n        assert \"brew install deno\" in guide\n        assert \"brew install node", "in guide\n\n    @patch('app.plugins.builtin.youtube_plugin.platform.system')\n    def test_get_runtime_installation_guide_windows(self, mock_system):\n        \"", "Test that Windows-specific installation guide is provided.\"", "\n        mock_system.return_value = \"Windows\"\n\n        plugin = YouTubePlugin()\n        guide = plugin._get_runtime_installation_guide()\n\n        assert \"Windows Installation:\" in guide\n        assert \"deno.land\" in guide\n        assert \"nodejs.org", "in guide\n\n    @patch('app.plugins.builtin.youtube_plugin.platform.system')\n    def test_get_runtime_installation_guide_linux(self, mock_system):\n        \"", "Test that Linux-specific installation guide is provided.\"", "\n        mock_system.return_value = \"Linux\"\n\n        plugin = YouTubePlugin()\n        guide = plugin._get_runtime_installation_guide()\n\n        assert \"Linux Installation:\" in guide\n        assert \"curl -fsSL\" in guide\n        assert \"install.sh\" in guide\n\n\nclass TestCookieManagement:\n    \"", "Test YouTube plugin's cookie file management.\"", "\n\n    def test_get_cookie_file_path_when_configured(self, youtube_plugin):\n        \"", "Test that cookie file path is retrieved from config when configured.\"", "\n        youtube_plugin.config[\"cookie_file_path"], ["cookie_file_path"], ["cookie_file_path"], {"title": "Test Video", "channel_name": "Test Channel", "video_format": "mp4", "video_quality": "best"}, ["download] Destination: /downloads/test.mp4", "stderr=\"", "await youtube_plugin._download_video(source)\n\n            # Check that JS runtime was added to command\n            calls = mock_run.call_args_list\n            for call in calls:\n                cmd = call[0][0] if call[0] else []\n                if \"--js-runtimes\" in cmd:\n                    idx = cmd.index(\"--js-runtimes", "assert cmd[idx + 1] == \"deno\"\n                    break\n            else:\n                pytest.fail(\"JS runtime not found in command", "pytest.mark.asyncio\n    async def test_download_includes_cookies_when_configured(\n        self, youtube_plugin\n    ):\n        \"", "Test that download command includes cookies when configured.\"", "\n        youtube_plugin.config[\"cookie_file_path"], {"title": "Test Video", "channel_name": "Test Channel", "video_format": "mp4", "video_quality": "best"}, ["download] Destination: /downloads/test.mp4", "stderr=\"", "await youtube_plugin._download_video(source)\n\n            # Check that cookies were added to command\n            calls = mock_run.call_args_list\n            for call in calls:\n                cmd = call[0][0] if call[0] else []\n                if \"--cookies\" in cmd:\n                    idx = cmd.index(\"--cookies", "assert cmd[idx + 1] == \"/path/to/cookies.txt\"\n                    break\n            else:\n                pytest.fail(\"Cookies not found in command", "pytest.mark.asyncio\n    async def test_download_excludes_cookies_when_invalid(self, youtube_plugin):\n        \"", "Test that download excludes cookies when file is invalid.\"", "\n        youtube_plugin.config[\"cookie_file_path"], {"title": "Test Video", "channel_name": "Test Channel", "video_format": "mp4", "video_quality": "best"}, ["download] Destination: /downloads/test.mp4", "stderr=\"", "await youtube_plugin._download_video(source)\n\n            # Check that cookies were NOT added to command\n            calls = mock_run.call_args_list\n            for call in calls:\n                cmd = call[0][0] if call[0] else []\n                assert \"--cookies", "not in cmd"]]